#include <stdio.h>
#include "euler.h"

int main() {
  printf("The sum of all multiples of 3 or 5 below 1000 is %d", multiples());
  printf("\nThe difference between the sum of the squares of the first one hundred natural numbers and the square of the sum is %d", sumSquare());
  return 0;
}
